window.KNIGHT_SCHOOL = Object.freeze({
  // Leave blank when the site is deployed on Vercel: the frontend will use /api/coach.
  // If you keep the frontend on GitHub Pages and host the API elsewhere,
  // paste that full endpoint here, for example:
  // apiUrl: "https://your-project.vercel.app/api/coach"
  apiUrl: "",
  brand: "Knight School with Clyde"
});
